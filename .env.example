# GoTunnel Server Configuration

# Domain for the tunnel service (required)
GOTUNNEL_DOMAIN=tunnel.example.com

# Server ports
GOTUNNEL_ADMIN_PORT=8080
GOTUNNEL_PROXY_PORT=80

# Secret key for session encryption (change this!)
GOTUNNEL_SECRET=change-me-to-a-random-secret-key

# Database configuration
# Supported types: sqlite (default) or mysql
GOTUNNEL_DB_TYPE=mysql

# SQLite database path (used when GOTUNNEL_DB_TYPE=sqlite)
# GOTUNNEL_SQLITE_DB_PATH=./data/gotunnel.db

# MySQL database connection string (used when GOTUNNEL_DB_TYPE=mysql)
# Format: username:password@tcp(host:port)/dbname
GOTUNNEL_MYSQL_DSN=root:rootpassword@tcp(localhost:3306)/tunnel

# Enable registration (set to false to disable public registration)
GOTUNNEL_ALLOW_REGISTRATION=true

# TLS (optional - use when not behind a reverse proxy)
# GOTUNNEL_TLS_CERT=/path/to/cert.pem
# GOTUNNEL_TLS_KEY=/path/to/key.pem
